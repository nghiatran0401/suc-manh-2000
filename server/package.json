{
  "name": "server",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "server": "cp .env.local .env && nodemon index.js",
    "server:windows": "copy .env.local .env && nodemon index.js",
    "server:local-docker-redis": "docker-compose -f docker-compose.redis.yaml up -d",
    "deploy:server": "cp .env.prod .env && firebase deploy --only functions"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase": "^10.11.1",
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1",
    "ioredis": "^5.4.1",
    "lodash": "^4.17.21",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
